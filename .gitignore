# ─────────────────────────────────────────────────────────────
# Chronvyr 源码仓库 .gitignore
#
# 本仓库的根目录就是 src/Chronvyr，只提交源码与工程描述文件。
#
# 实测这个目录的体积构成：
#   bin/      224.7 MB   (534 个文件)
#   obj/      105.9 MB   (414 个文件)
#   其余源码     0.63 MB  ( 83 个文件)
# 也就是说，忽略掉 bin/ 与 obj/ 之后，仓库总共不到 1 MB。
# ─────────────────────────────────────────────────────────────

# ── .NET 构建产物（完全可由源码重建，不要提交）──
bin/
obj/
*.binlog

# ── 编辑器 / IDE ──
.vs/
.idea/
*.user
*.suo

# ── 运行期文件 ──
*.log
